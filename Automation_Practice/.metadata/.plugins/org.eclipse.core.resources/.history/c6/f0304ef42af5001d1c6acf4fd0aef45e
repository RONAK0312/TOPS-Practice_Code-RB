package com.core;

import java.util.Scanner;

public class Sample {
	
	public static void main(String[] args) {
		//simple addition
		int a,b,c;
		a=10;
		b=20;
		c=a+b;
		System.out.println("The sum is :"+c);
		
		// Area of Circle with static value
		double pi=3.14, r=30.33, area;
		area = pi*r*r;
		System.out.println("Area of circle is :"+area);
		
		// Area of Circle with Dynamic variable
		Scanner sc=new Scanner(System.in);
		double pi=3.14, radious, area1;
		System.out.println("Enter Radious :"+area1);
		radious=sc.nextDouble();	
		area1 = pi*r*r;
		System.out.println("Area of circle is :"+area1);
	}
	
}
